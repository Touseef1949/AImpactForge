# Core framework
agno

# Web UI
streamlit

# LLM provider
groq

# Database interaction
# psycopg-binary is often easier for installation than psycopg
psycopg-binary
pgvector

# Environment variable loading
python-dotenv

# Ollama interaction (likely needed by agno.embedder.ollama)
ollama

# Add any other direct dependencies if discovered during testing
# e.g., numpy, requests might be pulled in by agno, but pip will handle them.
